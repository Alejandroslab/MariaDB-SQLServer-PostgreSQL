you can create table:

CREATE TABLE mytable (
-> id PRIMARY KEY INT NOT NULL AUTO_INCREMENT
-> name VARCHAR(5)
-> mobile VARCHAR(20)
-> );


CREATE TABLE Newtable
(ID key primary key,
Surname char(30) not null,
Name char(20) not null,
Address char(20 default)
)
