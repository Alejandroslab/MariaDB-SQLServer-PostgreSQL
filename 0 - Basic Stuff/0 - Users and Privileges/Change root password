Root password is empty after installation.

to change it:

enter in mysql or mariadb with:

sudo mysql -u root -p

(remmeber you must use -p to insert the password. just press enter as after the installation the password is empty)


now:

ALTER USER 'root'@'localhost' IDENTIFIED BY 'MyNewPass';