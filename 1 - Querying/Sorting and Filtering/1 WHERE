SELECT last_name, school
FROM teachers
WHERE school = 'New York School';

You can also use AND and OR:

SELECT last_name, school,
FROM teachers
WHERE school = 'New York School' AND salary < 40000;

SELECT last_name, school,
FROM teachers
where (address2 is null OR address2 = ‘ ‘) OR (postal_code = ‘ ‘ or phone = ‘ ‘);
